---
phase: 04-dashboard-en-tiempo-real
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - NodeAlert-Frontend/src/components/AppBar.tsx
  - NodeAlert-Frontend/src/components/Sidebar.tsx
  - NodeAlert-Frontend/src/components/ChartsPanel.tsx
  - NodeAlert-Frontend/src/components/TimeFilter.tsx
  - NodeAlert-Frontend/src/components/TempChart.tsx
  - NodeAlert-Frontend/src/components/HumidityChart.tsx
  - NodeAlert-Frontend/src/components/AlertPanel.tsx
  - NodeAlert-Frontend/src/components/EventTable.tsx
  - NodeAlert-Frontend/src/components/AlarmSound.tsx
  - NodeAlert-Frontend/src/pages/DashboardPage.tsx
  - NodeAlert-Frontend/src/pages/DeviceListPage.tsx
  - NodeAlert-Frontend/src/pages/DeviceDetailPage.tsx
  - NodeAlert-Frontend/src/App.tsx
autonomous: true
requirements: [UI-03, UI-04]
user_setup: []
must_haves:
  truths:
    - "ChartsPanel muestra gráficos Recharts LineChart de temperatura y humedad con TimeFilter (1h/6h/24h/7d)"
    - "TimeFilter controla timestamp__gte en la consulta API de readings"
    - "AlertPanel lista eventos críticos con severity badge (info/warning/critical) y acciones de resolución"
    - "EventTable permite filtrar por severity y marcar eventos como resueltos"
    - "AlarmSound reproduce beep simple mientras alarmActive=true (UI-SPEC D-08)"
    - "AppBar muestra logo, título NodeAlert IoT, y botón de logout"
    - "Sidebar navega entre Dashboard y Devices"
    - "DeviceListPage lista dispositivos registrados con estado online/offline"
    - "DeviceDetailPage muestra telemetría detallada de un dispositivo específico"
    - "DashboardPage integra SummaryBar + ChartsPanel + AlertPanel en layout completo"
  artifacts:
    - path: NodeAlert-Frontend/src/components/ChartsPanel.tsx
      provides: Recharts LineChart panel con TimeFilter
      contains: "LineChart"
    - path: NodeAlert-Frontend/src/components/AlertPanel.tsx
      provides: Eventos con severity badge y filtros
      contains: "severity"
    - path: NodeAlert-Frontend/src/components/AlarmSound.tsx
      provides: Beep de alarma mientras alarmActive
      contains: "Audio"
---

<objective>
Implementar los paneles restantes del dashboard: gráficos históricos de telemetría con Recharts, panel de eventos/alertas, alarma sonora, navegación (AppBar + Sidebar), y páginas de gestión de dispositivos.

Purpose: Completar el dashboard con visualización de históricos, alertas, y navegación completa.

Output: ChartsPanel, TimeFilter, TempChart, HumidityChart, AlertPanel, EventTable, AlarmSound, AppBar, Sidebar, DeviceListPage, DeviceDetailPage, DashboardPage final, App.tsx actualizado.
</objective>

<execution_context>
@/home/sebastian/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/sebastian/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-en-tiempo-real/04-CONTEXT.md
@.planning/phases/04-dashboard-en-tiempo-real/04-UI-SPEC.md

Existing files to read:
@NodeAlert-Frontend/src/App.tsx
@NodeAlert-Frontend/src/pages/DashboardPage.tsx
@NodeAlert-Frontend/src/context/ReadingsContext.tsx
@NodeAlert-Frontend/src/context/AlarmContext.tsx
@NodeAlert-Frontend/src/services/polling.ts
@NodeAlert-Backend/core/views.py
@NodeAlert-Backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChartsPanel + TimeFilter + Recharts temperature/humidity line charts</name>
  <files>
    NodeAlert-Frontend/src/components/TimeFilter.tsx
    NodeAlert-Frontend/src/components/TempChart.tsx
    NodeAlert-Frontend/src/components/HumidityChart.tsx
    NodeAlert-Frontend/src/components/ChartsPanel.tsx
  </files>

  <read_first>
    # UI-SPEC §2 for ChartsPanel props and states
    @.planning/phases/04-dashboard-en-tiempo-real/04-UI-SPEC.md

    # ReadingsContext to understand data shape for chart consumption
    @NodeAlert-Frontend/src/context/ReadingsContext.tsx

    # polling.ts for the data fetch functions
    @NodeAlert-Frontend/src/services/polling.ts
  </read_first>

  <action>
    Create `NodeAlert-Frontend/src/components/TimeFilter.tsx`:
    ```tsx
    import { ToggleButton, ToggleButtonGroup } from '@mui/material'

    export type TimeRange = '1h' | '6h' | '24h' | '7d'

    interface TimeFilterProps {
      value: TimeRange
      onChange: (range: TimeRange) => void
    }

    const OPTIONS: { value: TimeRange; label: string }[] = [
      { value: '1h', label: '1H' },
      { value: '6h', label: '6H' },
      { value: '24h', label: '24H' },
      { value: '7d', label: '7D' },
    ]

    export default function TimeFilter({ value, onChange }: TimeFilterProps) {
      return (
        <ToggleButtonGroup
          value={value}
          exclusive
          onChange={(_, val) => val && onChange(val)}
          size="small"
          sx={{ mb: 2 }}
        >
          {OPTIONS.map((opt) => (
            <ToggleButton key={opt.value} value={opt.value}>
              {opt.label}
            </ToggleButton>
          ))}
        </ToggleButtonGroup>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/TempChart.tsx`:
    ```tsx
    import { Card, CardContent, Typography } from '@mui/material'
    import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
    import type { Reading } from '../types'

    interface TempChartProps {
      data: Reading[]
    }

    export default function TempChart({ data }: TempChartProps) {
      const chartData = data
        .filter((r) => r.sensor_type === 'temperature')
        .slice(0, 100)
        .reverse()
        .map((r) => ({
          time: new Date(r.timestamp).toLocaleTimeString(),
          temperatura: r.value,
        }))

      return (
        <Card>
          <CardContent>
            <Typography variant="subtitle1" gutterBottom>Temperatura (°C)</Typography>
            <ResponsiveContainer width="100%" height={250}>
              <LineChart data={chartData}>
                <CartesianGrid strokeDasharray="3 3" stroke="#30363D" />
                <XAxis dataKey="time" stroke="#8B949E" fontSize={11} />
                <YAxis domain={[0, 50]} stroke="#8B949E" fontSize={11} />
                <Tooltip
                  contentStyle={{ backgroundColor: '#161B22', border: '1px solid #30363D' }}
                  labelStyle={{ color: '#E6EDF3' }}
                />
                <Line type="monotone" dataKey="temperatura" stroke="#FF9800" strokeWidth={2} dot={false} />
              </LineChart>
            </ResponsiveContainer>
          </CardContent>
        </Card>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/HumidityChart.tsx`:
    ```tsx
    import { Card, CardContent, Typography } from '@mui/material'
    import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts'
    import type { Reading } from '../types'

    interface HumidityChartProps {
      data: Reading[]
    }

    export default function HumidityChart({ data }: HumidityChartProps) {
      const chartData = data
        .filter((r) => r.sensor_type === 'humidity')
        .slice(0, 100)
        .reverse()
        .map((r) => ({
          time: new Date(r.timestamp).toLocaleTimeString(),
          humedad: r.value,
        }))

      return (
        <Card>
          <CardContent>
            <Typography variant="subtitle1" gutterBottom>Humedad (%)</Typography>
            <ResponsiveContainer width="100%" height={250}>
              <LineChart data={chartData}>
                <CartesianGrid strokeDasharray="3 3" stroke="#30363D" />
                <XAxis dataKey="time" stroke="#8B949E" fontSize={11} />
                <YAxis domain={[0, 100]} stroke="#8B949E" fontSize={11} />
                <Tooltip
                  contentStyle={{ backgroundColor: '#161B22', border: '1px solid #30363D' }}
                  labelStyle={{ color: '#E6EDF3' }}
                />
                <Line type="monotone" dataKey="humedad" stroke="#03A9F4" strokeWidth={2} dot={false} />
              </LineChart>
            </ResponsiveContainer>
          </CardContent>
        </Card>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/ChartsPanel.tsx`:
    ```tsx
    import { useState, useMemo } from 'react'
    import { Grid, Card, CardContent, Typography } from '@mui/material'
    import TimeFilter, { type TimeRange } from './TimeFilter'
    import TempChart from './TempChart'
    import HumidityChart from './HumidityChart'
    import { useReadings } from '../context/ReadingsContext'

    function getTimeFilterMs(range: TimeRange): number {
      const now = Date.now()
      switch (range) {
        case '1h': return now - 3600000
        case '6h': return now - 6 * 3600000
        case '24h': return now - 24 * 3600000
        case '7d': return now - 7 * 86400000
      }
    }

    export default function ChartsPanel() {
      const { readings } = useReadings()
      const [timeRange, setTimeRange] = useState<TimeRange>('1h')

      const filteredReadings = useMemo(() => {
        const cutoff = getTimeFilterMs(timeRange)
        return readings.filter((r) => new Date(r.timestamp).getTime() >= cutoff)
      }, [readings, timeRange])

      return (
        <Card sx={{ mb: 2 }}>
          <CardContent>
            <Typography variant="h6" gutterBottom>Historial de Lecturas</Typography>
            <TimeFilter value={timeRange} onChange={setTimeRange} />
            <Grid container spacing={2}>
              <Grid item xs={12} md={6}>
                <TempChart data={filteredReadings} />
              </Grid>
              <Grid item xs={12} md={6}>
                <HumidityChart data={filteredReadings} />
              </Grid>
            </Grid>
          </CardContent>
        </Card>
      )
    }
    ```
  </action>

  <verify>
    <automated>
      test -f NodeAlert-Frontend/src/components/TimeFilter.tsx
      test -f NodeAlert-Frontend/src/components/TempChart.tsx
      test -f NodeAlert-Frontend/src/components/HumidityChart.tsx
      test -f NodeAlert-Frontend/src/components/ChartsPanel.tsx
      grep -c "ToggleButtonGroup" NodeAlert-Frontend/src/components/TimeFilter.tsx
      grep -c "LineChart" NodeAlert-Frontend/src/components/TempChart.tsx
      grep -c "LineChart" NodeAlert-Frontend/src/components/HumidityChart.tsx
      grep -c "filteredReadings" NodeAlert-Frontend/src/components/ChartsPanel.tsx
    </automated>
  </verify>

  <acceptance_criteria>
    - TimeFilter.tsx renderiza ToggleButtonGroup con opciones 1H/6H/24H/7D
    - TempChart.tsx filtra readings por sensor_type='temperature', renderiza LineChart con eje Y 0-50, stroke #FF9800
    - HumidityChart.tsx filtra readings por sensor_type='humidity', renderiza LineChart con eje Y 0-100, stroke #03A9F4
    - ChartsPanel.tsx usa useReadings, filtra por timeRange, renderiza Grid con TempChart + HumidityChart
  </acceptance_criteria>

  <done>
    ChartsPanel funcional con TimeFilter y gráficos Recharts de temperatura y humedad.
  </done>
</task>

<task type="auto">
  <name>Task 2: AlertPanel + EventTable + ActionSound + AppBar + Sidebar</name>
  <files>
    NodeAlert-Frontend/src/components/EventTable.tsx
    NodeAlert-Frontend/src/components/AlertPanel.tsx
    NodeAlert-Frontend/src/components/AlarmSound.tsx
    NodeAlert-Frontend/src/components/AppBar.tsx
    NodeAlert-Frontend/src/components/Sidebar.tsx
  </files>

  <read_first>
    # UI-SPEC §2 for AlertPanel columns and severity badges
    @.planning/phases/04-dashboard-en-tiempo-real/04-UI-SPEC.md

    # AlarmContext for alarmActive
    @NodeAlert-Frontend/src/context/AlarmContext.tsx

    # ReadingsContext for events data
    @NodeAlert-Frontend/src/context/ReadingsContext.tsx

    # Existing DashboardPage to wrap with layout
    @NodeAlert-Frontend/src/pages/DashboardPage.tsx
  </read_first>

  <action>
    Create `NodeAlert-Frontend/src/components/EventTable.tsx`:
    ```tsx
    import { useState, useMemo } from 'react'
    import {
      Table, TableBody, TableCell, TableContainer, TableHead, TableRow,
      Chip, IconButton, Select, MenuItem, FormControl, InputLabel, Box,
    } from '@mui/material'
    import CheckCircleIcon from '@mui/icons-material/CheckCircle'
    import type { Event } from '../types'

    interface EventTableProps {
      events: Event[]
      onResolve: (id: number) => void
    }

    const SEVERITY_COLORS: Record<string, 'info' | 'warning' | 'error'> = {
      info: 'info',
      warning: 'warning',
      critical: 'error',
    }

    export default function EventTable({ events, onResolve }: EventTableProps) {
      const [filterSeverity, setFilterSeverity] = useState<string>('all')

      const filtered = useMemo(() => {
        if (filterSeverity === 'all') return events
        return events.filter((e) => e.severity === filterSeverity)
      }, [events, filterSeverity])

      return (
        <Box>
          <FormControl size="small" sx={{ mb: 2, minWidth: 120 }}>
            <InputLabel>Severidad</InputLabel>
            <Select value={filterSeverity} label="Severidad" onChange={(e) => setFilterSeverity(e.target.value)}>
              <MenuItem value="all">Todas</MenuItem>
              <MenuItem value="info">Info</MenuItem>
              <MenuItem value="warning">Warning</MenuItem>
              <MenuItem value="critical">Critical</MenuItem>
            </Select>
          </FormControl>
          <TableContainer>
            <Table size="small">
              <TableHead>
                <TableRow>
                  <TableCell>Timestamp</TableCell>
                  <TableCell>Severidad</TableCell>
                  <TableCell>Tipo</TableCell>
                  <TableCell>Mensaje</TableCell>
                  <TableCell>Acción</TableCell>
                </TableRow>
              </TableHead>
              <TableBody>
                {filtered.length === 0 && (
                  <TableRow>
                    <TableCell colSpan={5} align="center">No hay eventos registrados</TableCell>
                  </TableRow>
                )}
                {filtered.map((event) => (
                  <TableRow key={event.id}>
                    <TableCell>{new Date(event.timestamp).toLocaleString()}</TableCell>
                    <TableCell>
                      <Chip label={event.severity} color={SEVERITY_COLORS[event.severity]} size="small" />
                    </TableCell>
                    <TableCell>{event.event_type}</TableCell>
                    <TableCell>{event.message}</TableCell>
                    <TableCell>
                      {!event.resolved && (
                        <IconButton size="small" onClick={() => onResolve(event.id)} title="Marcar resuelto">
                          <CheckCircleIcon fontSize="small" />
                        </IconButton>
                      )}
                    </TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          </TableContainer>
        </Box>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/AlertPanel.tsx`:
    ```tsx
    import { useState } from 'react'
    import { Card, CardContent, Typography, Alert as MuiAlert } from '@mui/material'
    import EventTable from './EventTable'
    import { useReadings } from '../context/ReadingsContext'
    import api from '../services/api'

    export default function AlertPanel() {
      const { events } = useReadings()
      const [localEvents, setLocalEvents] = useState(events)

      const currentEvents = events.length > 0 ? events : localEvents

      const handleResolve = async (id: number) => {
        try {
          await api.patch(`/events/${id}/`, { resolved: true })
          setLocalEvents((prev) => prev.filter((e) => e.id !== id))
        } catch {
          // Silently fail
        }
      }

      return (
        <Card>
          <CardContent>
            <Typography variant="h6" gutterBottom>Eventos y Alertas</Typography>
            {currentEvents.length === 0 && (
              <MuiAlert severity="info">No hay eventos registrados</MuiAlert>
            )}
            <EventTable events={currentEvents} onResolve={handleResolve} />
          </CardContent>
        </Card>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/AlarmSound.tsx`:
    ```tsx
    import { useEffect, useRef } from 'react'
    import { useAlarm } from '../context/AlarmContext'

    export default function AlarmSound() {
      const { alarmActive } = useAlarm()
      const audioRef = useRef<HTMLAudioElement | null>(null)

      useEffect(() => {
        if (!audioRef.current) {
          audioRef.current = new Audio('data:audio/wav;base64,UklGRnoGAABXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YQoGAACAf39/f4B/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f3+AgH9/f38=')
          audioRef.current.loop = true
          audioRef.current.volume = 0.3
        }

        if (alarmActive) {
          audioRef.current.play().catch(() => {})
        } else {
          audioRef.current.pause()
          audioRef.current.currentTime = 0
        }

        return () => {
          if (audioRef.current) {
            audioRef.current.pause()
          }
        }
      }, [alarmActive])

      return null
    }
    ```

    Create `NodeAlert-Frontend/src/components/AppBar.tsx`:
    ```tsx
    import MuiAppBar from '@mui/material/AppBar'
    import Toolbar from '@mui/material/Toolbar'
    import Typography from '@mui/material/Typography'
    import Button from '@mui/material/Button'
    import { useAuth } from '../context/AuthContext'
    import { useAlarm } from '../context/AlarmContext'

    export default function AppBar() {
      const { logout } = useAuth()
      const { alarmActive } = useAlarm()

      return (
        <MuiAppBar position="static" sx={{ bg: alarmActive ? '#3A1B1B' : undefined }}>
          <Toolbar>
            <Typography variant="h6" sx={{ flexGrow: 1, fontFamily: "'Roboto Mono', monospace" }}>
              NodeAlert IoT
            </Typography>
            <Button color="inherit" onClick={logout}>Cerrar Sesión</Button>
          </Toolbar>
        </MuiAppBar>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/components/Sidebar.tsx`:
    ```tsx
    import { Drawer, List, ListItem, ListItemButton, ListItemIcon, ListItemText } from '@mui/material'
    import DashboardIcon from '@mui/icons-material/Dashboard'
    import DevicesIcon from '@mui/icons-material/Devices'
    import { useNavigate } from 'react-router-dom'

    const DRAWER_WIDTH = 220

    export default function Sidebar() {
      const navigate = useNavigate()

      return (
        <Drawer
          variant="permanent"
          sx={{
            width: DRAWER_WIDTH,
            flexShrink: 0,
            '& .MuiDrawer-paper': { width: DRAWER_WIDTH, boxSizing: 'border-box' },
          }}
        >
          <Toolbar />
          <List>
            <ListItem disablePadding>
              <ListItemButton onClick={() => navigate('/')}>
                <ListItemIcon><DashboardIcon /></ListItemIcon>
                <ListItemText primary="Dashboard" />
              </ListItemButton>
            </ListItem>
            <ListItem disablePadding>
              <ListItemButton onClick={() => navigate('/devices')}>
                <ListItemIcon><DevicesIcon /></ListItemIcon>
                <ListItemText primary="Dispositivos" />
              </ListItemButton>
            </ListItem>
          </List>
        </Drawer>
      )
    }

    /* Re-export Toolbar from MUI for layout spacing */
    import Toolbar from '@mui/material/Toolbar'
    export { DRAWER_WIDTH, Toolbar }
    ```
  </action>

  <verify>
    <automated>
      test -f NodeAlert-Frontend/src/components/EventTable.tsx
      test -f NodeAlert-Frontend/src/components/AlertPanel.tsx
      test -f NodeAlert-Frontend/src/components/AlarmSound.tsx
      test -f NodeAlert-Frontend/src/components/AppBar.tsx
      test -f NodeAlert-Frontend/src/components/Sidebar.tsx
      grep -c "EventTable" NodeAlert-Frontend/src/components/AlertPanel.tsx
      grep -c "Audio" NodeAlert-Frontend/src/components/AlarmSound.tsx
      grep -c "logout" NodeAlert-Frontend/src/components/AppBar.tsx
    </automated>
  </verify>

  <acceptance_criteria>
    - EventTable.tsx renderiza tabla con columnas Timestamp/Severidad/Tipo/Mensaje/Acción, filtro por severidad, y botón de resolver
    - AlertPanel.tsx usa Events de ReadingsContext, renderiza EventTable o "No hay eventos" si vacío
    - AlarmSound.tsx reproduce beep loop mientras alarmActive=true con data URI WAV
    - AppBar.tsx muestra título "NodeAlert IoT" + botón "Cerrar Sesión", fondo rojo cuando alarmActive
    - Sidebar.tsx renderiza Drawer permanente con links a Dashboard y Dispositivos
  </acceptance_criteria>

  <done>
    AlertPanel, EventTable, AlarmSound, AppBar y Sidebar implementados.
  </done>
</task>

<task type="auto">
  <name>Task 3: DeviceListPage + DeviceDetailPage + DashboardPage final + App.tsx routing</name>
  <files>
    NodeAlert-Frontend/src/pages/DeviceListPage.tsx
    NodeAlert-Frontend/src/pages/DeviceDetailPage.tsx
    NodeAlert-Frontend/src/pages/DashboardPage.tsx
    NodeAlert-Frontend/src/App.tsx
  </files>

  <read_first>
    # UI-SPEC §1.2 for route map
    @.planning/phases/04-dashboard-en-tiempo-real/04-UI-SPEC.md

    # Current App.tsx for route structure
    @NodeAlert-Frontend/src/App.tsx

    # Backend Device model fields for display
    @NodeAlert-Backend/core/models.py

    # Backend views for API endpoints
    @NodeAlert-Backend/core/views.py
  </read_first>

  <action>
    Create `NodeAlert-Frontend/src/pages/DeviceListPage.tsx`:
    ```tsx
    import { useEffect, useState } from 'react'
    import { Container, Typography, Card, CardContent, Grid, Chip, Box, CircularProgress } from '@mui/material'
    import { useNavigate } from 'react-router-dom'
    import api from '../services/api'
    import type { Device } from '../types'

    export default function DeviceListPage() {
      const navigate = useNavigate()
      const [devices, setDevices] = useState<Device[]>([])
      const [loading, setLoading] = useState(true)

      useEffect(() => {
        api.get<Device[]>('/devices')
          .then((res) => setDevices(res.data))
          .catch(() => {})
          .finally(() => setLoading(false))
      }, [])

      if (loading) return <Box sx={{ display: 'flex', justifyContent: 'center', mt: 4 }}><CircularProgress /></Box>

      return (
        <Container maxWidth="lg" sx={{ py: 3 }}>
          <Typography variant="h4" gutterBottom>Dispositivos</Typography>
          {devices.length === 0 && <Typography color="text.secondary">No hay dispositivos registrados</Typography>}
          <Grid container spacing={2}>
            {devices.map((d) => (
              <Grid item xs={12} sm={6} md={4} key={d.id}>
                <Card sx={{ cursor: 'pointer' }} onClick={() => navigate(`/device/${d.id}`)}>
                  <CardContent>
                    <Typography variant="h6">{d.name}</Typography>
                    <Typography variant="body2" color="text.secondary">ID: {d.device_id}</Typography>
                    <Typography variant="body2" color="text.secondary">Ubicación: {d.location || '—'}</Typography>
                    <Box sx={{ mt: 1 }}>
                      <Chip
                        label={d.is_active ? 'Activo' : 'Inactivo'}
                        color={d.is_active ? 'success' : 'default'}
                        size="small"
                      />
                    </Box>
                  </CardContent>
                </Card>
              </Grid>
            ))}
          </Grid>
        </Container>
      )
    }
    ```

    Create `NodeAlert-Frontend/src/pages/DeviceDetailPage.tsx`:
    ```tsx
    import { useEffect, useState } from 'react'
    import { Container, Typography, Card, CardContent, Grid, Box, CircularProgress } from '@mui/material'
    import { useParams } from 'react-router-dom'
    import TempChart from '../components/TempChart'
    import HumidityChart from '../components/HumidityChart'
    import api from '../services/api'
    import type { Device, Reading } from '../types'

    export default function DeviceDetailPage() {
      const { id } = useParams()
      const [device, setDevice] = useState<Device | null>(null)
      const [readings, setReadings] = useState<Reading[]>([])
      const [loading, setLoading] = useState(true)

      useEffect(() => {
        Promise.all([
          api.get<Device>(`/devices/${id}`),
          api.get<Reading[]>('/readings', { params: { device_id: id, ordering: '-timestamp', limit: 100 } }),
        ])
          .then(([d, r]) => {
            setDevice(d.data)
            setReadings(r.data)
          })
          .catch(() => {})
          .finally(() => setLoading(false))
      }, [id])

      if (loading) return <Box sx={{ display: 'flex', justifyContent: 'center', mt: 4 }}><CircularProgress /></Box>
      if (!device) return <Container><Typography>Dispositivo no encontrado</Typography></Container>

      return (
        <Container maxWidth="lg" sx={{ py: 3 }}>
          <Typography variant="h4" gutterBottom>{device.name}</Typography>
          <Typography variant="body2" color="text.secondary" gutterBottom>
            ID: {device.device_id} | MAC: {device.mac_address || '—'} | Ubicación: {device.location || '—'}
          </Typography>
          <Grid container spacing={2} sx={{ mt: 1 }}>
            <Grid item xs={12} md={6}>
              <TempChart data={readings} />
            </Grid>
            <Grid item xs={12} md={6}>
              <HumidityChart data={readings} />
            </Grid>
          </Grid>
        </Container>
      )
    }
    ```

    Update `NodeAlert-Frontend/src/pages/DashboardPage.tsx` — final full layout:
    ```tsx
    import { Box } from '@mui/material'
    import AppBar from '../components/AppBar'
    import Sidebar, { DRAWER_WIDTH } from '../components/Sidebar'
    import SummaryBar from '../components/SummaryBar'
    import ChartsPanel from '../components/ChartsPanel'
    import AlertPanel from '../components/AlertPanel'
    import AlarmSound from '../components/AlarmSound'

    export default function DashboardPage() {
      return (
        <Box sx={{ display: 'flex' }}>
          <AlarmSound />
          <AppBar />
          <Sidebar />
          <Box
            component="main"
            sx={{
              flexGrow: 1,
              ml: `${DRAWER_WIDTH}px`,
              mt: '64px',
              p: 3,
            }}
          >
            <SummaryBar />
            <ChartsPanel />
            <AlertPanel />
          </Box>
        </Box>
      )
    }
    ```

    Update `NodeAlert-Frontend/src/App.tsx` — add device routes and layout:
    ```tsx
    import { Routes, Route, Navigate } from 'react-router-dom'
    import { AuthProvider } from './context/AuthContext'
    import { ReadingsProvider } from './context/ReadingsContext'
    import { AlarmProvider } from './context/AlarmContext'
    import LoginPage from './pages/LoginPage'
    import DashboardPage from './pages/DashboardPage'
    import DeviceListPage from './pages/DeviceListPage'
    import DeviceDetailPage from './pages/DeviceDetailPage'
    import ProtectedRoute from './components/ProtectedRoute'

    export default function App() {
      return (
        <AuthProvider>
          <ReadingsProvider>
            <AlarmProvider>
              <Routes>
                <Route path="/login" element={<LoginPage />} />
                <Route
                  path="/"
                  element={
                    <ProtectedRoute>
                      <DashboardPage />
                    </ProtectedRoute>
                  }
                />
                <Route
                  path="/devices"
                  element={
                    <ProtectedRoute>
                      <DeviceListPage />
                    </ProtectedRoute>
                  }
                />
                <Route
                  path="/device/:id"
                  element={
                    <ProtectedRoute>
                      <DeviceDetailPage />
                    </ProtectedRoute>
                  }
                />
                <Route path="*" element={<Navigate to="/" replace />} />
              </Routes>
            </AlarmProvider>
          </ReadingsProvider>
        </AuthProvider>
      )
    }
    ```
  </action>

  <verify>
    <automated>
      test -f NodeAlert-Frontend/src/pages/DeviceListPage.tsx
      test -f NodeAlert-Frontend/src/pages/DeviceDetailPage.tsx
      grep -c "api.get.*/devices" NodeAlert-Frontend/src/pages/DeviceListPage.tsx
      grep -c "useParams" NodeAlert-Frontend/src/pages/DeviceDetailPage.tsx
      grep -c "ChartsPanel" NodeAlert-Frontend/src/pages/DashboardPage.tsx
      grep -c "AlertPanel" NodeAlert-Frontend/src/pages/DashboardPage.tsx
      grep -c "DeviceListPage" NodeAlert-Frontend/src/App.tsx
      grep -c "DeviceDetailPage" NodeAlert-Frontend/src/App.tsx
    </automated>
  </verify>

  <acceptance_criteria>
    - DeviceListPage.tsx carga devices de GET /api/v1/devices, renderiza Grid de Cards con nombre/ID/ubicación/estado
    - DeviceDetailPage.tsx carga device + readings para un ID específico, muestra TempChart + HumidityChart
    - DashboardPage.tsx layout final: AppBar + Sidebar + main area con SummaryBar + ChartsPanel + AlertPanel + AlarmSound
    - App.tsx añade rutas /devices y /device/:id envueltas en ProtectedRoute
  </acceptance_criteria>

  <done>
    Dashboard completo con navegación, gráficos, alertas, y gestión de dispositivos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend (charts) ↔ Backend (historical readings) | GET /api/v1/readings con token auth. Datos de telemetría. |
| Frontend (events) ↔ Backend (event management) | PATCH /api/v1/events/{id} para resolver eventos. Token auth. |
| Audio (data URI) ↔ Browser | Beep WAV embebido como data URI. Sin descarga externa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | Event resolution | mitigate | PATCH /events/{id}/ requiere autenticación Token. Sin verificación de ownership en v1 (acceptable para MVP). |
| T-04-03-02 | Tampering | Gráficos (datos de readings) | accept | Datos de solo lectura. GET /api/v1/readings es read-only (ReadOnlyModelViewSet). |
| T-04-03-03 | Information Disclosure | Device list expuesta | accept | Solo usuarios autenticados ven dispositivos. GET /devices requiere token. |
| T-04-03-04 | Denial of Service | Consultas históricas pesadas | accept | Django pagination limita resultados. MVP: límite 100 readings por consulta implícito. |
| T-04-03-05 | Spoofing | Audio data URI manipulada | mitigate | WAV embebido en código fuente (pinned en git). No se genera dinámicamente. |
</threat_model>

<verification>
- Task 1: Recharts LineChart renderiza con datos de contexto, TimeFilter cambia rango
- Task 2: EventTable filtrable, AlarmSound reproduce en critical, Sidebar navega
- Task 3: DeviceList carga de API, DeviceDetail con parámetro de ruta
- Build: `npm run build --prefix NodeAlert-Frontend` compila sin errores
- (Opcional) Verificar navegación completa: login → dashboard → devices → device detail
</verification>

<success_criteria>
1. ChartsPanel renderiza gráficos de temperatura (naranja) y humedad (azul) con Recharts
2. TimeFilter cambia el rango de tiempo de los gráficos (1h/6h/24h/7d)
3. AlertPanel lista eventos con severity badge y permite resolverlos
4. AlarmSound reproduce beep mientras haya alarma activa
5. AppBar + Sidebar permiten navegación entre Dashboard y Dispositivos
6. DeviceListPage muestra todos los dispositivos registrados
7. DeviceDetailPage muestra gráficos detallados para un dispositivo específico
8. `npm run build` compila sin errores de TypeScript
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-en-tiempo-real/04-03-SUMMARY.md` when done
</output>
