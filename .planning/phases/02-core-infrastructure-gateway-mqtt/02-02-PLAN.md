---
phase: 02-core-infrastructure-gateway-mqtt
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - docker-compose.yml
  - docker-compose.override.yml
  - NodeAlert-Backend/Dockerfile
  - NodeAlert-Backend/requirements.txt
  - NodeAlert-Backend/entrypoint.sh
  - NodeAlert-Backend/manage.py
  - NodeAlert-Backend/nodealert/__init__.py
  - NodeAlert-Backend/nodealert/settings.py
  - NodeAlert-Backend/nodealert/urls.py
  - NodeAlert-Backend/nodealert/wsgi.py
  - NodeAlert-Backend/core/__init__.py
  - NodeAlert-Backend/core/admin.py
  - NodeAlert-Backend/core/apps.py
  - NodeAlert-Backend/core/models.py
  - NodeAlert-Backend/core/serializers.py
  - NodeAlert-Backend/core/views.py
  - NodeAlert-Backend/core/urls.py
autonomous: true
requirements: [API-01, API-04, API-05, INFRA-01]
user_setup: []

must_haves:
  truths:
    - "Django server responds on port 8000 (inside container) mapped to host port 8000"
    - "MySQL stores data persistently in a named volume"
    - "API returns devices list at GET /api/v1/devices with pagination"
    - "API returns readings list at GET /api/v1/readings with sensor_type, device, and time-range filters"
    - "API returns events list at GET /api/v1/events with severity and resolved filters"
    - "API supports full CRUD (POST/PUT/DELETE) on /api/v1/devices and /api/v1/events"
    - "Database schema has exactly 4 tables: devices, readings, events, users"
    - "Readings table uses sensor_type ENUM (temperature, humidity, gas, flame)"
    - "Events table uses severity ENUM (info, warning, critical) with resolved flag"
  artifacts:
    - path: "NodeAlert-Backend/core/models.py"
      provides: "Django ORM models for Device, Reading, Event, User"
      contains: "class Device"
      contains: "class Reading"
      contains: "class Event"
    - path: "NodeAlert-Backend/core/serializers.py"
      provides: "DRF serializers with validation"
      contains: "class DeviceSerializer"
      contains: "class ReadingSerializer"
      contains: "class EventSerializer"
    - path: "NodeAlert-Backend/core/views.py"
      provides: "DRF viewsets with pagination and filtering"
      contains: "class DeviceViewSet"
      contains: "class ReadingViewSet"
      contains: "class EventViewSet"
    - path: "NodeAlert-Backend/core/urls.py"
      provides: "URL routing under /api/v1/"
      contains: "api/v1/devices"
      contains: "api/v1/readings"
      contains: "api/v1/events"
    - path: "NodeAlert-Backend/nodealert/settings.py"
      provides: "Django configuration (MySQL database, DRF, auth apps)"
      contains: "rest_framework"
      contains: "mysql"
    - path: "NodeAlert-Backend/Dockerfile"
      provides: "Docker build instructions for Django"
      exists: true
    - path: "NodeAlert-Backend/entrypoint.sh"
      provides: "Container startup (migrations + server)"
      exists: true
  key_links:
    - from: "docker-compose.yml"
      to: "NodeAlert-Backend/Dockerfile"
      via: "build context"
      pattern: "build: ./NodeAlert-Backend"
    - from: "NodeAlert-Backend/core/models.py"
      to: "NodeAlert-Backend/core/serializers.py"
      via: "import models"
      pattern: "from.*models import"
    - from: "NodeAlert-Backend/core/serializers.py"
      to: "NodeAlert-Backend/core/views.py"
      via: "import serializers"
      pattern: "serializers"
    - from: "NodeAlert-Backend/core/urls.py"
      to: "NodeAlert-Backend/nodealert/urls.py"
      via: "include"
      pattern: "include"

---

<objective>
Desplegar el backend Django con base de datos MySQL y API REST funcional, almacenando lecturas de sensores, eventos y dispositivos.

Purpose: Implementar el backend de servidor completo (Django + MySQL + REST API) como fuente de datos para el dashboard web (Fase 4) y el sistema de ingesta MQTT (Fase 3). Este plan entrega un backend funcional con 4 modelos de datos y endpoints API versionados.

Output:
- NodeAlert-Backend/ — proyecto Django (nodealert) con app core
- Dockerfile + entrypoint.sh — contenedor Django listo para Docker Compose
- docker-compose.yml actualizado — servicios Django + MySQL agregados
- docker-compose.override.yml actualizado — overrides de desarrollo Django
- 4 modelos: Device, Reading, Event, User (AbstractUser)
- 3 viewsets: devices, readings, events con serializers, paginación, filtros
- Migraciones de base de datos iniciales
</objective>

<execution_context>
@/home/sebastian/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/sebastian/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-infrastructure-gateway-mqtt/02-CONTEXT.md
@.planning/phases/02-core-infrastructure-gateway-mqtt/02-01-PLAN.md

# Depends on Plan 02-01: docker-compose.yml, .env.example, .gitignore already exist.
# NodeAlert-Firmware/src/config/mqtt_config.h defines topic prefix "nodealert/"
# NodeAlert-Firmware/src/config/wifi_config.h defines WiFi placeholder

# Key decisions for this plan:
# D-02: All services on nodealert-net. Django connects to mosquitto:1883 and mysql:3306.
# D-04: Django exposed on 8000, MySQL stays internal (no host port).
# D-05: 4 tables: devices, readings, events, users
# D-06: Single readings table with sensor_type ENUM + value FLOAT + unit VARCHAR
# D-07: Single events table with severity ENUM (info/warning/critical) + resolved BOOL
# D-08: Single Django app "core"
# D-09: Project name "nodealert"
# D-10: NodeAlert-Backend/ directory
# D-12: DRF viewsets + serializers + pagination
# D-13: API prefix /api/v1/
# D-14: Endpoints: GET /api/v1/readings (with filters), CRUD /api/v1/devices, CRUD /api/v1/events
# D-15: MySQL not exposed to host
# D-19: Django reads .env for MQTT credentials (for Phase 3 subscription)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Django project scaffold with Dockerfile, update docker-compose.yml with Django + MySQL services</name>
  <files>
    docker-compose.yml
    docker-compose.override.yml
    NodeAlert-Backend/Dockerfile
    NodeAlert-Backend/requirements.txt
    NodeAlert-Backend/entrypoint.sh
    NodeAlert-Backend/manage.py
    NodeAlert-Backend/nodealert/__init__.py
    NodeAlert-Backend/nodealert/settings.py
    NodeAlert-Backend/nodealert/urls.py
    NodeAlert-Backend/nodealert/wsgi.py
    NodeAlert-Backend/core/__init__.py
    NodeAlert-Backend/core/apps.py
  </files>

  <read_first>
    @.planning/phases/02-core-infrastructure-gateway-mqtt/02-01-PLAN.md
    Read the existing docker-compose.yml structure to understand the base services, network, and volumes.
    @.planning/phases/02-core-infrastructure-gateway-mqtt/02-CONTEXT.md
    Read D-02, D-04, D-08, D-09, D-10, D-11, D-15, D-19 for service layout.
    @.env.example
    Read environment variable naming to keep settings.py aligned.
  </read_first>

  <action>
    Create the Django project under NodeAlert-Backend/ with the following exact structure:

    1. Create directory tree:
       NodeAlert-Backend/
       NodeAlert-Backend/nodealert/   (Django project config)
       NodeAlert-Backend/core/        (single app per D-08)

    2. Create requirements.txt:
       - django>=5.0,<5.1
       - djangorestframework>=3.15,<3.16
       - django-filter>=24.1,<25
       - mysqlclient>=2.2,<3
       - python-dotenv>=1.0,<2
       - gunicorn>=22.0,<23

    3. Create NodeAlert-Backend/manage.py — standard Django manage.py:
       - Set DJANGO_SETTINGS_MODULE="nodealert.settings"
       - Import execute_from_command_line from django.core.management

    4. Create NodeAlert-Backend/nodealert/__init__.py — empty file

    5. Create NodeAlert-Backend/nodealert/settings.py with:
       - SECRET_KEY read from DJANGO_SECRET_KEY env var (required, no fallback): SECRET_KEY = os.environ['DJANGO_SECRET_KEY']
       - DEBUG read from DJANGO_DEBUG env var (default "False"): DEBUG = os.environ.get('DJANGO_DEBUG', 'False') == 'True'
       - ALLOWED_HOSTS = ['*'] (MVP — on local LAN. Hardened in Phase 6)
       - INSTALLED_APPS = ['django.contrib.admin', 'django.contrib.auth', 'django.contrib.contenttypes', 'django.contrib.sessions', 'django.contrib.messages', 'django.contrib.staticfiles', 'rest_framework', 'django_filters', 'core']
       - Database: MySQL engine, with host='mysql' (container name per D-02), port=3306, name from MYSQL_DATABASE env var, user from MYSQL_USER env var, password from MYSQL_PASSWORD env var, root password from MYSQL_ROOT_PASSWORD env var
         Use os.environ.get() with sensible defaults for all DB variables so the app can start even if .env is not loaded yet.
       - STATIC_URL = 'static/'
       - DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
       - Load .env via python-dotenv at the top of settings.py: from dotenv import load_dotenv; load_dotenv()
       - Do NOT add REST_FRAMEWORK config yet — that goes in Task 3 when viewsets are added (otherwise DRF complains about missing settings when running collectstatic during build)

    6. Create NodeAlert-Backend/nodealert/urls.py — minimal:
       - Import admin
       - urlpatterns = [path('admin/', admin.site.urls)]
       - Do NOT add api/ includes — those are added in Task 3

    7. Create NodeAlert-Backend/nodealert/wsgi.py — standard Django WSGI config:
       - Set DJANGO_SETTINGS_MODULE="nodealert.settings"
       - Get WSGI application from django.core.wsgi

    8. Create NodeAlert-Backend/core/__init__.py — empty file

    9. Create NodeAlert-Backend/core/apps.py:
       - AppConfig with name='core', verbose_name='NodeAlert Core'

    10. Create NodeAlert-Backend/Dockerfile:
        - Base image: python:3.12-slim
        - Install system dependencies for mysqlclient: gcc, default-libmysqlclient-dev, pkg-config
        - WORKDIR /app
        - Copy requirements.txt and run pip install (layer caching)
        - Copy the rest of the project
        - Copy entrypoint.sh and make executable
        - Expose port 8000
        - CMD ["./entrypoint.sh"]
        - Do NOT use ENV for secrets — they come from docker-compose environment

    11. Create NodeAlert-Backend/entrypoint.sh:
        - #!/bin/bash
        - Run: python manage.py migrate --noinput
        - Then: python manage.py runserver 0.0.0.0:8000
        - set -e at top
        - Note: Phase 3 will add MQTT subscriber startup here; Phase 6 will switch to gunicorn

    12. Update docker-compose.yml — ADD these services to the existing file:
        - django service:
          - build: ./NodeAlert-Backend
          - container_name: django
          - env_file: .env (reads all env vars from .env)
          - ports: "8000:8000" (exposed to host per D-04)
          - depends_on: mysql (wait for MySQL), mosquitto (wait for MQTT)
          - networks: nodealert-net
          - restart: unless-stopped
          - volumes: (none by default — development mounts go in override)
        - mysql service:
          - image: mysql:8.0
          - container_name: mysql
          - env_file: .env (reads MYSQL_* vars)
          - volumes: mysql_data:/var/lib/mysql (named volume for persistence)
          - networks: nodealert-net
          - restart: unless-stopped
          - Do NOT expose ports to host (per D-15 — MySQL is internal only)
        - ADD mysql_data to the volumes: section (alongside existing mosquitto volumes)

    13. Update docker-compose.override.yml — ADD django development overrides:
        - django service override:
          - volumes: ./NodeAlert-Backend:/app (bind mount for hot reload)
          - environment: DJANGO_DEBUG=True (explicit override even if .env has it)

    IMPORTANT: Use Read to load existing docker-compose.yml and append to it, do NOT overwrite. Maintain the existing mosquitto service, network, and volumes exactly as they are.
  </action>

  <verify>
    <automated>
      # Verify Django project files exist
      test -f NodeAlert-Backend/manage.py || echo 'FAIL: manage.py'
      test -f NodeAlert-Backend/nodealert/settings.py || echo 'FAIL: settings.py'
      test -f NodeAlert-Backend/nodealert/urls.py || echo 'FAIL: urls.py'
      test -f NodeAlert-Backend/Dockerfile || echo 'FAIL: Dockerfile'
      test -f NodeAlert-Backend/requirements.txt || echo 'FAIL: requirements.txt'
      test -f NodeAlert-Backend/entrypoint.sh || echo 'FAIL: entrypoint.sh'
      test -f NodeAlert-Backend/core/apps.py || echo 'FAIL: core/apps.py'

      # Verify docker-compose.yml has all 3 services
      python3 -c "
import yaml
with open('docker-compose.yml') as f:
    dc = yaml.safe_load(f)
assert 'django' in dc['services'], 'Missing django service'
assert 'mysql' in dc['services'], 'Missing mysql service'
assert 'mosquitto' in dc['services'], 'Missing mosquitto service'
assert 'mysql_data' in dc.get('volumes', {}), 'Missing mysql_data volume'
assert dc['services']['mysql'].get('ports') is None or dc['services']['mysql'].get('ports') == [], 'MySQL has host ports exposed'
print('docker-compose.yml validated OK')
"

      # Verify Django settings has correct config
      grep -q 'django.db.models.BigAutoField' NodeAlert-Backend/nodealert/settings.py || echo 'FAIL: DEFAULT_AUTO_FIELD'
      grep -q 'rest_framework' NodeAlert-Backend/nodealert/settings.py || echo 'FAIL: rest_framework in INSTALLED_APPS'
      grep -q "'ENGINE': 'django.db.backends.mysql'" NodeAlert-Backend/nodealert/settings.py || echo 'FAIL: MySQL engine'
      grep -q "'HOST': 'mysql'" NodeAlert-Backend/nodealert/settings.py || echo 'FAIL: MySQL host'

      echo 'Validation complete'
    </automated>
  </verify>

  <done>
    Django project files created in NodeAlert-Backend/.
    docker-compose.yml has django + mysql services alongside existing mosquitto.
    docker-compose.override.yml has django dev overrides.
    Django settings configured for MySQL.
    MySQL service has no host port exposed (internal only).
  </done>

  <acceptance_criteria>
    - NodeAlert-Backend/ directory exists with manage.py, requirements.txt, Dockerfile, entrypoint.sh
    - NodeAlert-Backend/nodealert/settings.py exists with MySQL engine config (host: mysql, port: 3306)
    - INSTALLED_APPS includes rest_framework and core
    - docker-compose.yml services include django and mysql alongside existing mosquitto
    - mysql_data named volume is defined
    - MySQL service has no ports mapping (internal only per D-15)
    - docker-compose.override.yml has django service with /app bind mount for hot reload
    - All Python files are parseable by Python3 AST
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Django models (Device, Reading, Event, User) with migrations</name>
  <files>
    NodeAlert-Backend/core/models.py
    NodeAlert-Backend/core/admin.py
  </files>

  <read_first>
    @.planning/phases/02-core-infrastructure-gateway-mqtt/02-CONTEXT.md
    Read D-05, D-06, D-07 for exact schema decisions.
  </read_first>

  <behavior>
    - Test 1: Device model has fields: device_id (unique, max_length=50), name (max_length=100), location (max_length=200, blank=True, default=''), is_active (BooleanField, default=True), created_at (auto_now_add), updated_at (auto_now), __str__ returns name or device_id
    - Test 2: Reading model has fields: device (ForeignKey to Device, on_delete=CASCADE, related_name='readings'), sensor_type (CharField with choices=['temperature','humidity','gas','flame'], max_length=20), value (FloatField), unit (CharField max_length=20), timestamp (DateTimeField). __str__ returns "{device_id}/{sensor_type} @ {timestamp}"
    - Test 3: Event model has fields: device (ForeignKey to Device, on_delete=CASCADE, related_name='events'), event_type (CharField max_length=50), severity (CharField with choices=['info','warning','critical'], max_length=20), message (TextField), resolved (BooleanField default=False), timestamp (DateTimeField). __str__ returns "{severity}: {event_type} on {device_id}"
    - Test 4: User model extends AbstractUser (no extra fields). Uses Django's default auth model.
    - Test 5: All models are registered in admin.py with ModelAdmin for Device, Reading, Event
    - Test 6: Migration files are generated with `python manage.py makemigrations core` and verified to create exactly 4 tables
  </behavior>

  <action>
    Create NodeAlert-Backend/core/models.py with Django models following D-05, D-06, D-07:

    1. Import path: from django.db import models, from django.contrib.auth.models import AbstractUser

    2. SensorType choices constant as a list of tuples at module level (not inline, for reuse):
       SENSOR_TYPE_CHOICES = [('temperature', 'Temperature'), ('humidity', 'Humidity'), ('gas', 'Gas'), ('flame', 'Flame')]

    3. Severity choices constant:
       SEVERITY_CHOICES = [('info', 'Info'), ('warning', 'Warning'), ('critical', 'Critical')]

    4. Device model:
       - device_id = models.CharField(max_length=50, unique=True) — the ESP32 chip ID, used as MQTT topic segment
       - name = models.CharField(max_length=100)
       - location = models.CharField(max_length=200, blank=True, default='')
       - is_active = models.BooleanField(default=True)
       - created_at = models.DateTimeField(auto_now_add=True)
       - updated_at = models.DateTimeField(auto_now=True)
       - Meta: ordering = ['-created_at'], verbose_name_plural = 'devices'
       - __str__ returns self.name or self.device_id

    5. Reading model:
       - device = models.ForeignKey(Device, on_delete=models.CASCADE, related_name='readings')
       - sensor_type = models.CharField(max_length=20, choices=SENSOR_TYPE_CHOICES)
       - value = models.FloatField()
       - unit = models.CharField(max_length=20)
       - timestamp = models.DateTimeField()
       - Meta: ordering = ['-timestamp'], verbose_name_plural = 'readings', indexes = [models.Index(fields=['device', 'sensor_type']), models.Index(fields=['timestamp'])]
       - __str__ returns "{device.device_id}/{sensor_type} @ {timestamp}"

    6. Event model:
       - device = models.ForeignKey(Device, on_delete=models.CASCADE, related_name='events')
       - event_type = models.CharField(max_length=50)
       - severity = models.CharField(max_length=20, choices=SEVERITY_CHOICES)
       - message = models.TextField()
       - resolved = models.BooleanField(default=False)
       - timestamp = models.DateTimeField()
       - Meta: ordering = ['-timestamp'], verbose_name_plural = 'events', indexes = [models.Index(fields=['device', 'severity']), models.Index(fields=['resolved'])]
       - __str__ returns "[{severity}] {event_type} @ {device.device_id}"

    7. User model:
       - class User(AbstractUser): pass
       - No extra fields — uses default Django auth fields (username, password, email, etc.)
       - Meta: verbose_name_plural = 'users'

    8. Register models in NodeAlert-Backend/core/admin.py:
       - from django.contrib import admin
       - from .models import Device, Reading, Event, User
       - admin.site.register(Device) — use a simple ModelAdmin with list_display = ['device_id', 'name', 'location', 'is_active', 'created_at']
       - admin.site.register(Reading) — use a simple ModelAdmin with list_display = ['device', 'sensor_type', 'value', 'unit', 'timestamp'], list_filter = ['sensor_type', 'timestamp']
       - admin.site.register(Event) — use a simple ModelAdmin with list_display = ['device', 'event_type', 'severity', 'message', 'resolved', 'timestamp'], list_filter = ['severity', 'resolved']
       - admin.site.register(User) — use admin.ModelAdmin

    9. After models.py is created, cd NodeAlert-Backend and run:
       python manage.py makemigrations core
       python manage.py migrate --run-syncdb   (or just let makemigrations create the migration file)

    IMPORTANT: Do NOT create a custom User manager unless absolutely necessary. Django's AbstractUser provides everything needed for MVP. The User model with AbstractUser will prompt for a custom User model migration — this is expected.
  </action>

  <verify>
    <automated>
      # Verify models defined
      python3 -c "
import ast, sys
with open('NodeAlert-Backend/core/models.py') as f:
    tree = ast.parse(f.read())
classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and any(
    isinstance(b, ast.Attribute) and b.attr == 'Model' for b in n.bases
)]
assert 'Device' in classes, 'Missing Device model'
assert 'Reading' in classes, 'Missing Reading model'
assert 'Event' in classes, 'Missing Event model'
print(f'Models found: {classes}')
"
      # Verify migrations exist
      test -d NodeAlert-Backend/core/migrations || echo 'FAIL: migrations dir'
      ls NodeAlert-Backend/core/migrations/*.py 2>/dev/null | grep -q -v __init__ || echo 'FAIL: no migration files'
      echo 'Validation complete'
    </automated>
  </verify>

  <done>
    NodeAlert-Backend/core/models.py defines Device, Reading, Event, and User (AbstractUser) models with exact field sets per D-06/D-07.
    All models registered in admin.py.
    Migration files generated in core/migrations/.
  </done>

  <acceptance_criteria>
    - models.py defines class Device (models.Model) with device_id, name, location, is_active, created_at, updated_at
    - models.py defines class Reading (models.Model) with device (FK), sensor_type (choices: temperature/humidity/gas/flame), value (FloatField), unit, timestamp
    - models.py defines class Event (models.Model) with device (FK), event_type, severity (choices: info/warning/critical), message, resolved, timestamp
    - models.py defines class User(AbstractUser) with no extra fields
    - admin.py registers Device, Reading, Event, User
    - Migration files exist in core/migrations/ directory (not just __init__.py)
    - python3 AST parse of models.py succeeds
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create DRF serializers, viewsets, and API URLs with pagination and filtering</name>
  <files>
    NodeAlert-Backend/core/serializers.py
    NodeAlert-Backend/core/views.py
    NodeAlert-Backend/core/urls.py
    NodeAlert-Backend/nodealert/settings.py
    NodeAlert-Backend/nodealert/urls.py
  </files>

  <read_first>
    @.planning/phases/02-core-infrastructure-gateway-mqtt/02-CONTEXT.md
    Read D-12, D-13, D-14 for API design decisions.
    @NodeAlert-Backend/core/models.py
    Must read the created models for exact field names and types.
    @NodeAlert-Backend/nodealert/settings.py
    Must read existing settings to add DRF configuration.
  </read_first>

  <behavior>
    - Test 1: GET /api/v1/devices returns a paginated list of devices with fields: id, device_id, name, location, is_active, created_at, updated_at
    - Test 2: GET /api/v1/readings accepts query params ?sensor_type=temperature&device_id=abc&timestamp__gte=2024-01-01&timestamp__lte=2024-12-31 and returns filtered results
    - Test 3: POST /api/v1/devices creates a device and returns 201
    - Test 4: GET /api/v1/devices/{id}/ returns a single device
    - Test 5: PUT /api/v1/devices/{id}/ updates a device
    - Test 6: DELETE /api/v1/devices/{id}/ deletes a device and returns 204
    - Test 7: GET /api/v1/events?severity=critical&resolved=false returns filtered events
    - Test 8: All endpoints are under /api/v1/ prefix
    - Test 9: Default pagination is 50 items per page
  </behavior>

  <action>
    Create the DRF API layer:

    1. Create NodeAlert-Backend/core/serializers.py:
       - from rest_framework import serializers
       - from .models import Device, Reading, Event
       
       - DeviceSerializer(serializers.ModelSerializer):
         - class Meta: model=Device, fields='__all__', read_only_fields=['created_at', 'updated_at']
       
       - ReadingSerializer(serializers.ModelSerializer):
         - class Meta: model=Reading, fields='__all__'
         - device = serializers.CharField(source='device.device_id', read_only=True) — Will use PrimaryKeyRelatedField. Actually, for POST the reading is created via MQTT ingestion (Phase 3). For MVP, the readings endpoint is read-only per D-14. So use a read-only serializer.
         - Actually, per D-14: "Readings POST se añade en Fase 3 (ingesta MQTT desde Django)". So for this phase, ReadingSerializer is read-only (GET only). But to keep it simple, use ModelSerializer with fields='__all__' and rely on the viewset restricting POST.
         - Decision: Use a plain ModelSerializer with fields='__all__'. The viewset will handle read-only enforcement.
       
       - EventSerializer(serializers.ModelSerializer):
         - class Meta: model=Event, fields='__all__', read_only_fields=['timestamp']
         - timestamp is auto-set on creation if not provided

    2. Create NodeAlert-Backend/core/views.py:
       - from rest_framework import viewsets, filters
       - from django_filters.rest_framework import DjangoFilterBackend
       - from .models import Device, Reading, Event
       - from .serializers import DeviceSerializer, ReadingSerializer, EventSerializer

       - DeviceViewSet(viewsets.ModelViewSet):
         - queryset = Device.objects.all()
         - serializer_class = DeviceSerializer
         - filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
         - filterset_fields = ['is_active', 'location']
         - search_fields = ['name', 'device_id', 'location']
         - ordering_fields = ['created_at', 'name', 'device_id']
         - Full CRUD (ModelViewSet gives GET, POST, PUT, PATCH, DELETE automatically)

       - ReadingViewSet(viewsets.ReadOnlyModelViewSet):
         - queryset = Reading.objects.all()
         - serializer_class = ReadingSerializer
         - filter_backends = [DjangoFilterBackend, filters.OrderingFilter]
         - filterset_fields = ['sensor_type', 'device__device_id']  (note: device__device_id for filtering by device_id string)
         - For timestamp range filters, use django-filter's DateTimeFromToRangeFilter or declare filterset_fields with '__gte'/'__lte' lookup
         - Implementation: Define a custom Filterset class or use filterset_fields with lookups
         - Use django_filters.FilterSet subclass:
           ```
           class ReadingFilter(django_filters.FilterSet):
               sensor_type = django_filters.CharFilter(lookup_expr='exact')
               device_id = django_filters.CharFilter(field_name='device__device_id', lookup_expr='exact')
               timestamp__gte = django_filters.DateTimeFilter(field_name='timestamp', lookup_expr='gte')
               timestamp__lte = django_filters.DateTimeFilter(field_name='timestamp', lookup_expr='lte')
               class Meta:
                   model = Reading
                   fields = ['sensor_type', 'device_id', 'timestamp__gte', 'timestamp__lte']
           ```
           Then in the viewset: filterset_class = ReadingFilter
         - ordering_fields = ['timestamp']
         - Read-only (ReadOnlyModelViewSet blocks POST, PUT, DELETE)

       - EventViewSet(viewsets.ModelViewSet):
         - queryset = Event.objects.all()
         - serializer_class = EventSerializer
         - filter_backends = [DjangoFilterBackend, filters.OrderingFilter]
         - filterset_fields = ['severity', 'resolved']
         - ordering_fields = ['timestamp', 'severity']
         - Full CRUD

    3. Create NodeAlert-Backend/core/urls.py:
       - from django.urls import path, include
       - from rest_framework.routers import DefaultRouter
       - from .views import DeviceViewSet, ReadingViewSet, EventViewSet
       
       - router = DefaultRouter()
       - router.register(r'devices', DeviceViewSet, basename='device')
       - router.register(r'readings', ReadingViewSet, basename='reading')
       - router.register(r'events', EventViewSet, basename='event')
       
       - urlpatterns = router.urls

    4. Update NodeAlert-Backend/nodealert/urls.py:
       - Add: path('api/v1/', include('core.urls')) to urlpatterns
       - Keep existing admin path

    5. Update NodeAlert-Backend/nodealert/settings.py:
       - Add REST_FRAMEWORK dict to settings:
         ```
         REST_FRAMEWORK = {
             'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
             'PAGE_SIZE': 50,
             'DEFAULT_FILTER_BACKENDS': ['django_filters.rest_framework.DjangoFilterBackend'],
         }
         ```
       - Do NOT add authentication classes yet — that's Plan 03
       - Make sure django_filters is already in INSTALLED_APPS (added in Task 1)

    IMPORTANT: Use Read to load existing settings.py and urls.py before modifying. Insert the new config, don't overwrite the whole file.
  </action>

  <verify>
    <automated>
      EXIT_CODE=0
      test -f NodeAlert-Backend/core/serializers.py || { echo 'FAIL: serializers.py'; EXIT_CODE=1; }
      test -f NodeAlert-Backend/core/views.py || { echo 'FAIL: views.py'; EXIT_CODE=1; }
      test -f NodeAlert-Backend/core/urls.py || { echo 'FAIL: core/urls.py'; EXIT_CODE=1; }
      grep -q 'DeviceSerializer' NodeAlert-Backend/core/views.py || { echo 'FAIL: DeviceSerializer in views'; EXIT_CODE=1; }
      grep -q 'ReadingSerializer' NodeAlert-Backend/core/views.py || { echo 'FAIL: ReadingSerializer in views'; EXIT_CODE=1; }
      grep -q 'EventSerializer' NodeAlert-Backend/core/views.py || { echo 'FAIL: EventSerializer in views'; EXIT_CODE=1; }
      grep -q "devices" NodeAlert-Backend/core/urls.py || { echo 'FAIL: devices route'; EXIT_CODE=1; }
      grep -q "readings" NodeAlert-Backend/core/urls.py || { echo 'FAIL: readings route'; EXIT_CODE=1; }
      grep -q "events" NodeAlert-Backend/core/urls.py || { echo 'FAIL: events route'; EXIT_CODE=1; }
      grep -q "api/v1/" NodeAlert-Backend/nodealert/urls.py || { echo 'FAIL: api/v1/ in root urls'; EXIT_CODE=1; }
      grep -q 'PAGE_SIZE' NodeAlert-Backend/nodealert/settings.py || { echo 'FAIL: PAGE_SIZE in settings'; EXIT_CODE=1; }
      grep -q 'PageNumberPagination' NodeAlert-Backend/nodealert/settings.py || { echo 'FAIL: PageNumberPagination in settings'; EXIT_CODE=1; }
      [ "$EXIT_CODE" -eq 0 ] && echo 'Validation passed' || echo "Validation FAILED with $EXIT_CODE failure(s)"
      exit "$EXIT_CODE"
    </automated>
  </verify>

  <done>
    Serializers, viewsets (ModelViewSet/ReadOnlyModelViewSet), and router URLs created for devices, readings, events.
    ReadingViewSet is read-only (per D-14). DeviceViewSet and EventViewSet are full CRUD.
    Readings endpoint supports filter by sensor_type, device_id, and timestamp range (gte/lte).
    DRF configured with PageNumberPagination (50 per page).
    URLs mounted at /api/v1/ prefix.
  </done>

  <acceptance_criteria>
    - core/serializers.py defines DeviceSerializer, ReadingSerializer, EventSerializer as ModelSerializers
    - core/views.py defines DeviceViewSet(ModelViewSet), ReadingViewSet(ReadOnlyModelViewSet), EventViewSet(ModelViewSet)
    - ReadingViewSet has custom Filterset with sensor_type (exact), device_id (via device__device_id), timestamp__gte, timestamp__lte
    - core/urls.py uses DefaultRouter registering devices, readings, events
    - nodealert/urls.py includes core.urls at 'api/v1/'
    - settings.py has REST_FRAMEWORK with PageNumberPagination and PAGE_SIZE=50
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN → Django API (8000) | Untrusted HTTP clients can reach Django API on port 8000. MVP has no auth yet (added in Plan 03). |
| MySQL container → Docker network | MySQL is accessible only within the Docker bridge network (nodealert-net). No host port mapping. |
| Django → MySQL | Django connects to MySQL via container name (mysql:3306). Credentials from .env. |
| Django → Mosquitto | Django will connect to Mosquitto for MQTT subscription (Phase 3). Connection uses .env credentials. |
| API → Database | All database writes go through Django ORM, not raw SQL queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | Django API endpoints | accept (temporary) | API is open without authentication in Plan 02. This is an accepted temporary state — API-06 (token auth) is implemented in Plan 03, immediately following this plan. The API is on a local LAN only. |
| T-02-06 | Tampering | SQL injection via API | mitigate | DRF serializers validate input types and lengths. Django ORM uses parameterized queries — no raw SQL in MVP codebase. django-filter filters are scoped to allowed fields via filterset_fields / Filterset class. |
| T-02-07 | Information Disclosure | MySQL port exposed to host | mitigate | Per D-15, MySQL has NO host port mapping. Only accessible within the Docker bridge network (nodealert-net). |
| T-02-08 | Information Disclosure | Django SECRET_KEY in version control | mitigate | SECRET_KEY is read from DJANGO_SECRET_KEY env var (loaded from .env, which is gitignored). No hardcoded secrets in settings.py. |
| T-02-09 | Tampering | API mass assignment | mitigate | DRF ModelSerializer uses explicit fields='__all__' which includes all model fields but read_only_fields protect created_at/updated_at/timestamp. In Plan 03, authentication prevents unauthorized writes. |
| T-02-10 | Elevation of Privilege | Django admin unprotected | accept (temporary) | Admin interface is accessible at /admin/ without auth in Plan 02. Plan 03 adds login requirement via staff_user flag. Temporary state during the same phase. |
| T-02-SC | Tampering | python:3.12-slim image | mitigate | Verified official Python Docker image. mysql:8.0 is an official MySQL image with millions of pulls. |
</threat_model>

<verification>
- Docker image builds: `docker compose build django` exits 0
- Django can detect settings: `docker compose run --rm django python manage.py check` exits 0
- Models are valid: `docker compose run --rm django python manage.py makemigrations --check` exits 0 (no unapplied migrations)
- API responds: `docker compose up -d` then `curl -s http://localhost:8000/api/v1/devices/` returns 200 and a paginated JSON response
- Readings filter works: `curl "http://localhost:8000/api/v1/readings/?sensor_type=temperature"` returns 200
- All 4 tables in schema: `docker compose exec mysql mysql -u root -p"$MYSQL_ROOT_PASSWORD" nodealert -e "SHOW TABLES;"` shows devices, core_reading, core_event, core_user (Django prefixes app name)
</verification>

<success_criteria>
- [ ] `docker compose build django` exits 0
- [ ] `docker compose run --rm django python manage.py check` exits 0 (no config errors)
- [ ] `docker compose run --rm django python manage.py showmigrations` shows all migrations applied (marked [X])
- [ ] `curl -s http://localhost:8000/api/v1/devices/` returns 200 with `{"count":0,"next":null,"previous":null,"results":[]}`
- [ ] `curl -s http://localhost:8000/api/v1/readings/?sensor_type=temperature` returns 200
- [ ] `curl -X POST http://localhost:8000/api/v1/devices/ -H "Content-Type: application/json" -d '{"device_id":"esp32-01","name":"Living Room","location":"Office"}'` returns 201
- [ ] `curl -s http://localhost:8000/api/v1/devices/1/` returns 200 with the created device
- [ ] `curl -X DELETE http://localhost:8000/api/v1/devices/1/` returns 204
- [ ] MySQL has 4 tables (core_device, core_reading, core_event, core_user) verified via docker compose exec mysql
</success_criteria>

<output>
Create `.planning/phases/02-core-infrastructure-gateway-mqtt/02-02-SUMMARY.md` when done
</output>
