# Mosquitto generated password files — NOT ignored for mosquitto/config/
# (empty placeholder is tracked so clone works; setup.sh overwrites it)
# Ignore any stray copies outside of mosquitto/config/
/mosquitto_passwd

# Python cache
__pycache__/
*.pyc

# VS Code
.vscode/
IngenieriaEnComputacion2/
# macOS
.DS_Store
Molotica/
# Raspberry Pi — exclude firmware (ESP32, not needed on Pi)
NodeAlert-Firmware/
# Raspberry Pi — exclude planning docs (not needed on Pi)
.planning/

# Local development override — not for production (Raspberry Pi)
docker-compose.override.yml

# Django collectstatic artifacts (regenerated on deploy)
NodeAlert-Backend/static/admin/
NodeAlert-Backend/static/rest_framework/

# Python virtual environment
.venv/
NodeAlert-Backend/.venv/

# Frontend dependencies and build (rebuilt via Docker on deploy)
NodeAlert-Frontend/node_modules/
NodeAlert-Frontend/dist/
